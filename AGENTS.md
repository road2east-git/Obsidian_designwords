# AGENTS.md — Knowledge Wiki Schema

> 이 파일은 LLM이 위키를 유지보수할 때 따라야 할 규칙과 워크플로우를 정의합니다.
> 사용자와 LLM이 함께 발전시켜 나갑니다.

## 위키 구조

### 디렉토리 규칙
- `raw/` — 원본 소스. **절대 수정하지 않는다.** 읽기 전용.
- `raw/assets/` — 이미지 및 첨부파일 저장소.
- `wiki/sources/` — 각 원본 소스에 대한 요약 페이지.
- `wiki/entities/` — 사람, 조직, 도구, 프로젝트 등 고유 개체 페이지.
- `wiki/concepts/` — 개념, 이론, 방법론, 기술 등 추상적 주제 페이지.
- `wiki/analyses/` — 비교, 분석, 종합, 질문 답변 등 파생 콘텐츠.

### 파일 명명 규칙
- 파일명은 소문자, 하이픈 구분: `transformer-architecture.md`
- 한글 제목이 자연스러운 경우 한글 사용 허용: `트랜스포머-아키텍처.md`
- 모든 위키 페이지에는 YAML 프론트매터를 포함한다.

### 프론트매터 템플릿
```yaml
---
title: "페이지 제목"
type: source | entity | concept | analysis
created: YYYY-MM-DD
updated: YYYY-MM-DD
tags: [태그1, 태그2]
sources: [관련 소스 파일명]
---
```

## 워크플로우

### Ingest (소스 수집)
1. 사용자가 `raw/`에 새 소스를 추가한다.
2. LLM이 소스를 읽고 핵심 내용을 사용자와 논의한다.
3. `wiki/sources/`에 요약 페이지를 생성한다.
4. `index.md`에 새 항목을 추가한다.
5. 관련된 기존 `wiki/entities/`, `wiki/concepts/` 페이지를 업데이트한다.
6. 새 개체/개념이 발견되면 새 페이지를 생성한다.
7. `log.md`에 기록을 남긴다.
8. 포맷: `## [YYYY-MM-DD] ingest | 소스 제목`

### Query (질문)
1. 사용자가 질문을 한다.
2. `index.md`에서 관련 페이지를 찾는다.
3. 관련 위키 페이지를 읽고 종합 답변을 생성한다.
4. 가치 있는 답변은 `wiki/analyses/`에 새 페이지로 저장한다.
5. `log.md`에 기록을 남긴다.
8. 포맷: `## [YYYY-MM-DD] query | 질문 요약`

### Lint (검수)
1. 페이지 간 모순 확인
2. 최신 소스로 갱신되지 않은 오래된 주장 확인
3. 연결 없는 고아 페이지 식별
4. 언급만 되고 페이지가 없는 개념 발견
5. 누락된 교차 참조(`[[링크]]`) 보완
6. `log.md`에 기록을 남긴다.
7. 포맷: `## [YYYY-MM-DD] lint | 점검 요약`

## 교차 참조 규칙
- 위키 내부 링크는 Obsidian 형식 사용: `[[페이지명]]`
- 원본 소스 참조: `[소스명](../raw/파일명.md)`
- 모든 위키 페이지는 최소 1개 이상의 인바운드 링크를 가져야 한다.

## 언어
- 위키 콘텐츠는 기본적으로 **한국어**로 작성한다.
- 기술 용어는 필요 시 영문 병기: `트랜스포머(Transformer)`
